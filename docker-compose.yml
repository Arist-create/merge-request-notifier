version: '3.8'

services:
  merge-monitor:
    build: .
    container_name: merge-monitor
    restart: unless-stopped
    volumes:
      - ./merge_monitor.log:/app/merge_monitor.log
      - ./.env:/app/.env
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
